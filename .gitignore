# Each subproject (mobile/, backend/) has its own .gitignore for
# stack-specific entries (Expo, native build folders, dist/, etc).
# This file only covers things that can show up at the repo root.

node_modules/
.DS_Store
*.log
.env
.env*.local

# Cloudflare tunnel temp logs
/tmp/cf-tunnel-*.log
